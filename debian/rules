#!/usr/bin/make -f

export DEB_LDFLAGS_MAINT_APPEND = -Wl,--as-needed

%:
	dh $@  --with autotools-dev

override_dh_install:
	mkdir -p debian/conman/lib/systemd/system/
	cp debian/conman.service debian/conman/lib/systemd/system/

